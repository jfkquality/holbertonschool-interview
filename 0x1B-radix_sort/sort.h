#ifndef _SORT_H
#define _SORT_H

#include <stdio.h>
#include <stdlib.h>
#include <math.h>

void radix_sort(int *array, size_t size);
void print_array(const int *array, size_t size);

#endif /* _SORT_H */
